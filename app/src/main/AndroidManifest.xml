<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sabias_que">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Acerca"></activity>
        <activity
            android:name=".ui.login.Acerca"
            android:label="@string/title_activity_acerca" />
        <activity android:name=".Ayuda" />
        <activity android:name=".Informacion" />
        <activity android:name=".Curioso_Activity" />
        <activity android:name=".Historia_Activity" />
        <activity android:name=".Ciencia_Activity" />
        <activity android:name=".Principal_Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>